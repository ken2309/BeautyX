import React from 'react';
import { IOrganization } from '../../../interface/organization';

interface IProps {
    org: IOrganization
}

function OrgSearchItem(props: IProps) {
    const { org } = props;
    return (
        <div>

        </div>
    );
}

export default OrgSearchItem;